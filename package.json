{
  "name": "meal-planner",
  "version": "1.0.0",
  "description": "Simple weekly meal planner for baby and adult meals",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "seed": "node seed.js",
    "deploy": "rsync -avz --delete --exclude node_modules --exclude .git --exclude '*.db*' ./ root@192.168.50.193:~/meal-planner/"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.21.0",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "pino-pretty": "^11.0.0"
  },
  "optionalDependencies": {
    "pino-roll": "^1.3.0"
  }
}
